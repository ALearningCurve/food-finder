/**
 * Represents an error message that can thrown either by the API or some function
 * on the front end.
 */
export interface ErrorMessage {
  /**
   * The message of the error
   */
  errorMessage: string;
}
